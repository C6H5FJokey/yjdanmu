{
  "name": "yjdanmu",
  "version": "1.0.0",
  "description": "月计式弹幕机 - Tauri 版本",
  "scripts": {
    "dev": "cd src && npm run dev",
    "build": "cd src && npm run build",
    "tauri": "tauri",
    "tauri-dev": "concurrently \"cd src && npm run dev\" \"tauri dev -- --bin app\"",
    "tauri-build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.6",
    "concurrently": "^8.2.2"
  }
}